/**
 * Paquete controller — HTTP Controllers (Capa de Entrada)
 *
 * Responsabilidad: Recibir requests HTTP, extraer parámetros,
 * delegar a la capa Service y devolver respuestas JSON.
 *
 * Contenido futuro (Fase 5):
 *  - UserController    (Paso 5.1)
 *  - OrderController   (Paso 5.2)
 *  - DisputeController (Paso 5.3)
 *
 * Dependencia: → service.*
 */
package com.rappifavor.controller;
